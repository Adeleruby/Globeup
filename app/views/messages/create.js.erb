<% if @message.errors.any? %>
  // handle validation errors later
<% else %>

  var messagesContainer = document.querySelector("#chat-element-<%= @chat_room.id %>");
  messagesContainer.insertAdjacentHTML('beforeend', '<%= j render "message", message: @message, user_is_messages_author: true %>');
  var input = document.querySelector('#message_content<%= @chat_room.id %>');
  input.value = '';
  input.blur();
  scrollLastMessageIntoView();
<% end %>
