<div class="show-container">
  <div class="top-show-container">
    <div class="title-box">
      <h2> <%= @city.name %> </h2>
      <p><%= @city.location %></p>
      <p><%= @city.description %></p>
    </div>
    <div class="city-photo">
      <%#= cl_image_tag @city.photo, height: 400, width: 600, crop: :fill %>
    </div>
  </div>
</div>
